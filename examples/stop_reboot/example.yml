button:
  - platform: power_utils
    name: "Reboot"
    action: reboot

  - platform: power_utils
    name: "Shutdown"
    action: shutdown

  - platform: power_utils
    name: "Hibernate"
    action: hibernate

  - platform: power_utils
    name: "Logout"
    action: logout

  - platform: power_utils
    name: "Sleep"
    action: sleep